eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/
server:
  port: 8765
spring:
  application:
    name: service-feign

feign:  #打开feign的断路器，默认是关闭的
  hystrix:
    enabled: true